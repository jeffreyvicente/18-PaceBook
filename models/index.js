// Imports the User model
const User = require('./User');

// Imports the Thought model
const Thought = require('./Thought');

// Exports the imported modules as an object
module.exports = { User, Thought};